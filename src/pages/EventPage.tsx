import PageShell from "@/components/PageShell";

export default function EventPage() {
  return (
    <PageShell title="Event Schedule" description="Schedule agents based on events.">
      <p className="text-sm text-gray-500">No event-based schedules configured yet.</p>
    </PageShell>
  );
}

