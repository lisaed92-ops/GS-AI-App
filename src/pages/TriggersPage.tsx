import PageShell from "@/components/PageShell";

export default function TriggersPage() {
  return (
    <PageShell title="Triggers" description="Set up event-based triggers for your agents.">
      <p className="text-sm text-gray-500">No triggers configured yet.</p>
    </PageShell>
  );
}

